---
trigger: always_on
---

1. Do not use toast in the axiosInstance call outside for error we already have a toast in axiosInstance so no need your.
2. Use tanstact query, mutation for API calls management
3. For style of the element use border-none and shadow-none I dont't want border and show in my this thing
4. For api call don't use toast as we have one global toast in axiosInstance adding two places causes two display of message
